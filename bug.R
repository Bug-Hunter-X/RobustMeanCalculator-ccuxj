```R
# This code attempts to calculate the mean of a vector containing NA values without handling them appropriately.
my_vector <- c(1, 2, NA, 4, 5)
mean_value <- mean(my_vector)
print(mean_value)
```